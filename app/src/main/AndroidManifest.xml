<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.musicaldelight">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <application

        android:allowBackup="true"
        android:icon="@mipmap/imagia"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/imagia"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashActivity"
           >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity

            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".DesActivity"/>

        <activity android:name=".PlayMusic"/>
        <activity android:name=".VoiceCommand"/>
        <activity android:name=".Songs"/>
        <activity android:name=".Albums"/>
        <activity android:name=".Artists"/>
        <activity android:name=".SignOut"
            android:theme="@style/MyTheme"></activity>
        <activity android:name=".SignOutButton"/>

    </application>

</manifest>